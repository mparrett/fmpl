Write your research findings and create actionable subtasks.

1. Write a pattern doc to `docs/codebase/<topic>.md` with your discoveries.
2. Update AGENTS.md "Codebase Discovery Docs" section.
3. Create subtasks with `jj issue create` -- each MUST include code snippets and patterns so the next iteration can implement without re-exploring.
4. Commit with `jj describe`.

Only docs/codebase/ and AGENTS.md are writable in this state.
