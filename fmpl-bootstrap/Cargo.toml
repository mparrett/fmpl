[package]
name = "fmpl-bootstrap"
version = "0.1.0"
edition = "2024"
description = "Minimal FMPL interpreter for bootstrapping the parser generator"
authors = ["ndn"]
license = "MIT"

[[bin]]
name = "fmpl-bootstrap"
path = "src/main.rs"

[dependencies]
fmpl-core = { path = "../fmpl-core" }
